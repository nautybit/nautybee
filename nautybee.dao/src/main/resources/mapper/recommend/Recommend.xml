<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.nautybit.nautybee.dao.recommend.RecommendDao">

    <resultMap id = "BaseResultMap" type = "com.nautybit.nautybee.entity.recommend.Recommend"
               extends = "COMMON.BASE_RESULT_MAP">
        <result column = "to_user" property = "toUser" />
        <result column = "from_user" property = "fromUser" />
        <result column = "is_valid" property = "isValid" />
        <result column = "remark" property = "remark" />
    </resultMap>

    <sql id = "BaseWhereClause">
        <where>
            <trim prefixOverrides = "and">
                <include refid = "COMMON.BASE_WHERE_CLAUSE" />
                <if test = "toUser != null">
                    and to_user = #{toUser}
                </if>
                <if test = "fromUser != null">
                    and from_user = #{fromUser}
                </if>
                <if test = "isValid != null">
                    and is_valid = #{isValid}
                </if>
                <if test = "remark != null">
                    and remark = #{remark}
                </if>
            </trim>
        </where>
    </sql>

    <sql id = "BaseUpdateSet">
        <set>
            <include refid = "COMMON.BASE_UPDATE_SET" />
            <if test = "toUser != null">
                to_user = #{toUser},
            </if>
            <if test = "fromUser != null">
                from_user = #{fromUser},
            </if>
            <if test = "isValid != null">
                is_valid = #{isValid},
            </if>
            <if test = "remark != null">
                remark = #{remark}
            </if>
        </set>
    </sql>

    <!-- 查询总数 -->
    <select id = "selectCount" resultType = "java.lang.Integer">
        select count(id)
        from nautybee_recommend
        <include refid = "BaseWhereClause" />
    </select>

    <!-- 查询 -->
    <select id = "select" resultMap = "BaseResultMap">
        select
        <include refid = "COMMON.ALL_COLUMN_LIST" />
        from nautybee_recommend
        <include refid = "BaseWhereClause" />
        <include refid = "COMMON.ORDER_BY" />
        <include refid = "COMMON.PAGE" />
    </select>

    <!-- 根据ID查询 -->
    <select id = "selectById" resultMap = "BaseResultMap">
        select
        <include refid = "COMMON.ALL_COLUMN_LIST" />
        from nautybee_recommend
        where id = #{id}
    </select>

    <!-- 根据IDS批量删除 -->
    <update id = "deleteByIds">
        update nautybee_recommend
        set is_deleted='Y'
        where id in
        (
        <foreach collection = "array" index = "index" item = "tag" separator = ",">
            #{tag}
        </foreach>
        )
    </update>

    <!-- 根据ID删除 -->
    <update id = "deleteById">
        update nautybee_recommend
        set is_deleted='Y'
        where id=#{id}
    </update>

    <!-- 删除 -->
    <delete id = "delete">
        update nautybee_recommend
        set is_deleted='Y'
        <include refid = "BaseWhereClause" />
    </delete>

    <!-- 添加   -->
    <insert id = "insert" useGeneratedKeys = "true" keyProperty = "id">
        insert into nautybee_recommend (
        <include refid = "COMMON.BASE_INSERT_COLUMN" />
        to_user,
        from_user,
        is_valid,
        remark
        )
        values (
        <include refid = "COMMON.BASE_INSERT_VALUE" />
        #{toUser},
        #{fromUser},
        #{isValid},
        #{remark}
        )
    </insert>

    <!-- 通过ID更新 -->
    <update id = "updateById">
        update nautybee_recommend
        <include refid = "BaseUpdateSet" />
        where id = #{id}
    </update>

</mapper>
